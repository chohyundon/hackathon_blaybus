[build]
  command = "npm run build"
  publish = "dist"

# API 프록시: /api/* → be-dosa.store (CORS 회피)
[[redirects]]
  from = "/api/*"
  to = "https://be-dosa.store/:splat"
  status = 200
  force = true

# SPA 폴백
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
